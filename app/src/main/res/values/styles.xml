<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="BaseTheme">
        <!-- Customize your theme here. -->
    </style>

    <style name="BaseTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="colorPrimary">@color/photos_500_primary</item>
        <item name="colorPrimaryDark">@color/photos_500_primary_dark</item>
        <item name="colorControlHighlight">@color/photos_500_accent</item>
    </style>

</resources>
